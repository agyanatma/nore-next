import styled from "styled-components";

const Icon = styled.button`
    background-color: transparent;
    padding: 0px;
    outline: none;
    border: none;
    cursor: pointer;
`;

export default Icon;
